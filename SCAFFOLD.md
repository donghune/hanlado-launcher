# Scaffold trail

Generated by `@bitstone/create-launcher` on 2026-06-23T11:48:35.305Z.
Base launcher: `/Users/yudonghun/MinecraftProjects/blue-zone-launcher`

## Answers
```json
{
  "launcherId": "hanlado-launcher",
  "productName": "Hanlado Launcher",
  "appId": "hanladolauncher",
  "description": "한라농장 마인크래프트 런처",
  "author": "donghune",
  "homepage": "https://github.com/donghune/hanlado-launcher",
  "githubUser": "donghune",
  "githubRepo": "hanlado-launcher",
  "copyrightYear": 2026,
  "server": {
    "id": "hanlado-main",
    "name": "한라농장 서버",
    "description": "한라농장 - Fabric 1.21.4",
    "address": "localhost:25566",
    "minecraftVersion": "1.21.4",
    "icon": "https://raw.githubusercontent.com/donghune/hanlado-launcher/master/app/assets/images/SealCircle.png"
  },
  "modulePresets": [
    "fabric-1.21.4",
    "fabric-api-0.119.4+1.21.4",
    "fabric-language-kotlin-1.13.11",
    "sodium-iris",
    "complementary-r5.7.1",
    "korean-chat-patch-1.9.5",
    "blur-server-address-1.2.0"
  ],
  "extraModules": [
    {
      "id": "kr.donghune:hanlado_client:1.0-SNAPSHOT",
      "name": "Hanlado Mod",
      "type": "FabricMod",
      "artifact": {
        "size": 0,
        "MD5": "",
        "url": "https://github.com/donghune/hanlado-launcher/releases/download/v1.0.0/hanlado_client-1.0-SNAPSHOT.jar"
      }
    }
  ]
}
```

## Next steps
1. Replace branding assets under `app/assets/images/` (logo, SealCircle.png) so they match `Hanlado Launcher`.
2. Review `distribution.json` — mod jars referenced via GitHub Releases need real `size`/`MD5`.
3. Inspect the residuals listed below and edit by hand where needed.
4. `npm install && npm start` to dev-launch.

## Residuals from the base launcher (review by hand)

- `app/assets/lang/_custom.toml` — Blue Zone (literal)
- `app/assets/lang/_custom.toml` — BLUE ZONE (uppercase)
- `app/assets/lang/ko_KR.toml` — Blue Zone (literal)
- `distribution.json` — blue-zone-launcher (URL/path)

Notes:
- Game-specific lines (e.g. `welcomeHeader`, `welcomeDescription`, gameplay strings in `app/assets/lang/*.toml`) are intentionally left intact — they describe the *event*, not the launcher, so they need a manual rewrite for each new event.
- Shader/file URLs in `distribution.json` still point at the base launcher's GitHub Release. After uploading the same assets to `donghune/hanlado-launcher` releases, update those URLs.
